(func fibonacci (n (a 0) (b 1))
  (if (= n 0)
    nil
    (cons a (fibonacci (- n 1) b (+ a b)))))

(func each (f list) (while list (f (car list)) (setq list (cdr list))))

(each println (fibonacci 10))
